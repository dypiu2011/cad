- create ec2 instance and connect to ssh and run command


$ sudo yum update -y

$ sudo yum install docker -y

$ sudo systemctl start docker -y

$ sudo systemctl enable -y

$ docker --version

$ vi dockerfile
FROM nginx
RUN apt-get update
RUN apt-get install nginx -y
CMD ["nginx" , "-g", "daemon off;"]

$ sudo docker build -t myimage:latest .

$ sudo docker images

$ sudo docker run --name mycontainer -p 8080:80 -d myimage:latest

$ docker ps

$ copy the ec2 ip and paste it